# Project Structure

This repo is intentionally lean:

- `frontend/` for the player-facing UI
- `backend/` for validation, simulation, and judging APIs
- `content/` for game challenge data
- `docs/` for team alignment
